name: Run unit tests

on: [pull_request]
    
jobs:
  test:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v6
      - name: Running unit tests 🧪
        run: |
          go test ./data -v

